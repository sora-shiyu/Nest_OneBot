import { Injectable } from '@nestjs/common';
// import { GroupMsg, pokeMsg, privateMsg } from './bot/bot.dto';
// import { ApiService } from './api/api.service';

@Injectable()
export class AppService {
  // constructor(private readonly api: ApiService) {}
}
